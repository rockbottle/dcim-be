python-jose[cryptography] # To handle JWT-related logic in your auth files
passlib[bcrypt]          # To handle password hashing/verification during user tests
ruff==0.3.4              # Fast linter/formatter to ensure code stays clean
pytest>=9.0.3
pytest-asyncio>=0.23.5
httpx>==0.27.0
asyncpg